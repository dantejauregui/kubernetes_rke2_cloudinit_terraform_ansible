all:
  children:

    control_plane:
      hosts:

        cp1:
          ansible_host: 192.168.0.19

    workers:
      hosts:

        worker1:
          ansible_host: 192.168.0.21

        worker2:
          ansible_host: 192.168.0.20

  vars:

    ansible_user: ubuntu

    ansible_ssh_private_key_file: /Users/dante/.ssh/id_ed25519